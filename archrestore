#!/usr/bin/env bash
set -euo pipefail

# --- 1. Install packages ---

echo "Installing packages..."

sudo pacman -S --needed base-devel git rsync

if [[ -f "$HOME/pkglist.txt" ]]; then
    sudo pacman -S --needed - < "$HOME/pkglist.txt"
fi

if [[ -f "$HOME/aurlist.txt" ]]; then
    yay -S --needed - < "$HOME/aurlist.txt"
fi

# --- 2. Restore configs ---

echo "Restoring /etc..."
if [[ -d /mnt/backup/etc ]]; then
    sudo rsync -aAXv /mnt/backup/etc/ /etc/
fi

echo "Restoring /home..."
if [[ -d /mnt/backup/home ]]; then
    sudo rsync -aAXv /mnt/backup/home/ /home/
fi

# --- 3. Restore system services ---

if [[ -f "$HOME/services_state.txt" ]]; then
    echo "Restoring system services..."

    while read -r service state _; do
        case "$state" in
            enabled)
                sudo systemctl enable --now "$service"
                ;;
            disabled)
                sudo systemctl disable "$service"
                ;;
            masked)
                sudo systemctl mask "$service"
                ;;
        esac
    done < "$HOME/services_state.txt"
fi

echo "Rebuild complete!"

